import React from 'react';

import preloader from '../images/utensils-loader.gif';

const Preloader = () => {
  return (
    <img id='preloader' src={preloader}></img>
  );
};

export default Preloader;
